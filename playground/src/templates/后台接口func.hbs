/**
 * {{ summary }}
 * @param { {{ requestType }} } data
 * @param {import('axios').AxiosRequestConfig} config
 * @returns {Promise<{{ returnType }}>}
 */
export function {{ functionName }}(data, config = {}) {
  return request.request({
    method: '{{ method }}',
    url: '{{{ url }}}',
    {{#isPost}}data,{{/isPost}}{{#isGet}}params: data,{{/isGet}}
    ...config,
  })
}

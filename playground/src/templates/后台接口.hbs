/**
 * {{ summary }}
 * @param { {{ requestType }} } data
 * @param {import('axios').AxiosRequestConfig} config
 * @returns {Promise<{{ returnType }}>}
 */
export const {{ functionName }} = (data, config = {}) => {
  return request.request({
    method: '{{ method }}',
    url: '{{{ url }}}',
    data,
    ...config
  })
}
